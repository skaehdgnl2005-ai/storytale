# StoryTale — 개인화 아동 그림책 생성 앱

## 프로젝트 개요
부모가 서술형으로 원하는 바를 적으면, AI가 아동발달 가드레일 안에서
아이의 이름·특성이 녹아든 맞춤 그림책을 생성하는 서비스.

## 제품 철학

### 왜 만드는가
모든 아이는 자기가 주인공인 이야기를 들을 자격이 있다.
기성 동화책은 "어떤 아이"를 위해 쓰였지만, 이 책은 "바로 이 아이"를 위해 만들어진다.
부모가 아이에게 해주고 싶은 말을, 아이가 좋아하는 세계 안에서, 아이의 언어로 전해준다.

### 핵심 경험
- 부모는 전문가가 아니어도 된다. 1~2문장이면 충분하다. "요즘 동생 때문에 힘들어해요"만으로 한 권의 책이 된다.
- 아이는 책을 펼쳤을 때 "이건 나잖아!"라고 느껴야 한다. 이름, 토니(곰 인형), 서준이, 토끼 — 아이의 세계가 그대로 책 안에 있다.
- 그림체는 따뜻하고 안전해야 한다. 아이가 자기 전에 보는 책이다.

### 설계 원칙
- 부모의 자유 서술을 받되, 아동발달 가드레일(0층) 안에서만 생성한다. 주제는 무한하지만 품질 기준은 전문가 수준이다.
- AI가 만든 설계를 부모에게 먼저 보여준다. 부모가 확정하기 전까지 생성하지 않는다.
- 개인화는 자연스러워야 한다. 이름을 기계적으로 끼워넣는 느낌이면 실패다.
- 같은 캐릭터가 전 페이지에서 일관되게 보여야 한다(character consistency).

### 톤 & 보이스

**페르소나**: StoryTale은 "경험 많은 어린이집 선생님 같은 친구". 권위가 아닌 동행의 톤. 부모가 전문 용어를 몰라도 편하게 느끼도록.

- 부모에게: 따뜻하고 신뢰감 있게. "전문가가 함께하고 있어요"가 아니라 **"당신이 아이를 가장 잘 알아요"**의 톤.
- 아이에게(스토리 텍스트): 다정하고 리듬감 있게. 가르치지 않고 함께 경험하는 말투.
- UI 전반: 군더더기 없이 깔끔하되 차갑지 않게. 파스텔 톤 기반.

**카피 패턴**:
| 상황 | ❌ 쓰지 않는다 | ✅ 이렇게 쓴다 |
|------|---------------|---------------|
| 에러 | "오류가 발생했습니다" | "잠깐, 다시 한번 해볼게요 😊" |
| 로딩 | "생성 중..." | "이야기가 자라고 있어요 🌱" |
| 빈 상태 | "데이터가 없습니다" | "첫 번째 이야기를 만들어볼까요?" |
| 성공 | "완료되었습니다" | "이야기가 완성됐어요! 📖" |
| 입력 유도 | "내용을 입력하세요" | "어떤 이야기를 담아볼까요?" |

**규칙**: 이모지는 문장 끝에 1개만 (과다 사용 금지). 느낌표는 긍정 상황에서만. 아이 이름이 있으면 반드시 사용. "사용자" 등 시스템 용어 금지.

## 아키텍처 (4층 구조)
- **0층 가드레일**: 전문가 설계 감정흐름 템플릿, 연령별 문체, 안전규칙. AI의 교과서.
- **1층 AI 인터프리터**: 부모 텍스트 → 의도 분석 → 장면 설계(scene_plan) 실시간 생성.
- **2층 부모 확인 루프**: 설계 미리보기 → 수정 요청 → 확정. 최대 3회.
- **3층 개인화 생성**: 확정 scene_plan + 아이 프로필 → 장면별 텍스트 + 일러스트 프롬프트.
- **4층 AI 일러스트**: PuLID 얼굴 앵커 → 멀티뷰 캐릭터 시트 → Identity Prompt Block → 장면별 일러스트 → CLIP+DINOv2 하이브리드 검증.

## 기술 스택
- 모노레포: `packages/{shared, backend, mobile, admin-web}`
- 백엔드: FastAPI (Python 3.12), PostgreSQL, Redis
- 모바일: React Native (Expo)
- AI 텍스트: Claude API (Sonnet)
- AI 일러스트: Flux.1 Dev + PuLID(bytedance/flux-pulid) + IP-Adapter (Replicate API)
- 배포: Railway (백엔드), Expo EAS (모바일), GitHub Actions (CI/CD)

## 핵심 규칙
1. **TDD 필수.** 모든 구현 전에 테스트를 먼저 작성하라. 테스트 실패 확인 후 구현.
2. **계약 준수.** 모듈 간 통신은 `docs/contracts/`의 인터페이스를 따르라.
3. **작은 단위.** 한 세션에서 파일 5개 이상 동시 수정하지 마라.
4. **결정 기록.** 새 패턴이나 아키텍처 결정은 SESSION_LOG에 기록하라.
5. **프롬프트 버전 관리.** LLM 프롬프트 수정 시 `docs/prompts/`에 버전과 변경 이유 기록.

## 빌드 & 테스트
```bash
# 백엔드 (venv 활성화 필수)
cd packages/backend
.venv\Scripts\activate   # Windows
# source .venv/bin/activate  # macOS/Linux
pytest
ruff check src tests
ruff format src tests

# 공유 타입
cd packages/shared && npx tsc --noEmit

# 모바일
cd packages/mobile && npm test
```
## Design System
UI 작업 시 반드시 `/docs/visual-identity-guide-rn.md`의 Section 12 "AI 프롬프트용 요약"을 읽고 따를 것. 다크 모드·모션·컴포넌트 스타일 등 상세 스펙이 필요하면 전체 문서를 참조.

## 작업 방식
- 태스크 목록: `docs/TASK_BACKLOG.md`
- 세션 기록: `docs/SESSION_LOG.md`
- 상세 아키텍처: `docs/ARCHITECTURE.md`
- 인터페이스 계약: `docs/contracts/`
- LLM 프롬프트: `docs/prompts/`
- 가드레일 시드 데이터: `docs/guardrail-seeds/`
- 아트 디렉션 가이드: `docs/guardrail-seeds/art-direction.json` (일러스트 작업 시 필수 참조)

## 컨텍스트 로딩 전략 (새 세션 시작 시)
1. **항상 자동 로드**: 이 파일(CLAUDE.md) + `.claude/rules/` (IDE가 자동 처리)
2. **태스크 확인**: `docs/TASK_BACKLOG.md`에서 현재 태스크 정보 + `시작 시 필수 읽기` 문서 로드
3. **의존성 컨텍스트**: `docs/SESSION_LOG.md`에서 의존 태스크의 "구현 요약" 섹션 읽기
4. **계약 확인**: `docs/contracts/`에서 구현할 인터페이스 확인
5. **절대 하지 말 것**: 모든 문서를 한 번에 읽지 마라. 컨텍스트 윈도우를 낭비한다.
